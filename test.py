# from urllib.request import urlopen
# import json
# import pprint

# GBOOKS = "https://www.googleapis.com/books/v1/volumes?q=intitle:"

# resp = urlopen(f"{GBOOKS}confessions+of+an+economic+hitman")
# book_data = json.load(resp)

# add_book = book_data['items'][1]['volumeInfo']


# print((f"Title: {add_book.get('title', 'title not found')}\n"
#         f"Author: {add_book.get('authors', 'author not found')[0]}\n"
#         f"Pages: {add_book.get('pageCount', 'page count not found')}\n"
#         f"{", ".join(add_book.get('categories', ['genres not found']))}"))
 

print(len("description: "))
